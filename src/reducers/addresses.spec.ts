describe("Addresses reducers tests", () => {
	it("SEARCH_ADDRESS_LOADING should return correct values", () => {
		// TODO: write unit test
		expect().toEqual({});
	});
	it("SEARCH_ADDRESS_SUCCESS should return correct values", () => {
		// TODO: write unit test
		expect().toEqual({});
	});
	it("SEARCH_ADDRESS_ERROR should return correct values", () => {
		// TODO: write unit test
		expect().toEqual({});
	});
	it("ADD_ADDRESS_SEARCH should return correct values", () => {
		// TODO: write unit test
		expect().toEqual({});
	});
});
