describe("Address validation", () => {
	it("should check if address is valid", () => {
		// TODO: write unit test
		expect().toEqual({});
	});

	it("should check if short address is invalid", () => {
		// TODO: write unit test
		expect().toEqual({});
	});

	it("should check if incorrect address is invalid", () => {
		// TODO: write unit test
		expect().toEqual({});
	});
});
