describe("ApiService tests", () => {
	it("should run two requests on activation", () => {
		// TODO: write unit test
		expect().toEqual({});
	});

	it("should format resulting data correctly", () => {
		// TODO: write unit test
		expect().toEqual({});
	});

	it("should handle api errors with correct response", () => {
		// TODO: write unit test
		expect().toEqual({});
	});
});
